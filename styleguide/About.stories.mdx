import { Meta } from "@storybook/blocks";

<Meta title="About/About" />

# React Components

This component library includes all the components that are used to build [http://kits.se](http://kits.se) and
other React applications that we create and use at KITS.

To install the library, run `npm install @kokitotsos/react-components --save`
