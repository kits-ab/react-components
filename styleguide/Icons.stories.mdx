import { ArgsTable, Meta } from "@storybook/addon-docs/blocks";
import { Horizontal, types } from "../src"
import * as icons from "../src/icons"

<Meta title="Design/Icons" />

# Icons

All icons that can be used.

<style>{`
  .container {
    margin: 0 -10px;
  }

  .container > * {
    box-sizing: border-box;
    flex: 0 1 calc(100% / 3 - 20px);
    margin-left: 10px;
    margin-right: 10px;
    margin-bottom: 10px;
    margin-top: 10px;
  }


  @media (max-width: 900px) {
    .container > * {
      flex: 0 1 calc(50% - 20px);
    }
  }

  @media (max-width: 600px) {
    .container > * {
      flex: 0 1 calc(100% - 20px);
    }
  }

  .icon {
    background-color: #f8f8f8;
    border-radius: 4px;
    border: 1px solid rgba(0,0,0,.1);
    box-shadow: rgba(0,0,0,0.10) 0 1px 3px 0;
    overflow: hidden;
  }

  .icon-icon {
    padding: 20px;
  }

  .icon-name {
    background-color: #ffffff;
    font-family: Consolas, 'Liberation Mono', Menlo, monospace;
    font-size: 13px;
    flex: 1;
    line-height: 40px;
    padding: 20px;
    vertical-align: middle;
  }
`}</style>

<Horizontal wrapRows={true} className="container">
  {Object.keys(icons).map(icon => (
    <div className="icon">
      <Horizontal alignVertical={types.Alignment.Center}>
        <div className="icon-icon">
          {icons[icon]({ fill: "#f37f4a", height: 32, width: 32 })}
        </div>
        <div className="icon-name">{`<${icon}/>`}</div>
      </Horizontal>
    </div>
  ))}
</Horizontal>

<ArgsTable of={icons.AirplaneIcon} />
